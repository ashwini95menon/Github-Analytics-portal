import csv
import shutil

with open("reposelect_csvfile.csv") as csv_file:
    csv_reader = csv.reader(csv_file, delimiter = ",")
    for row in csv_reader:
        for val in row:
            shutil.copy("data_gen/bugs_" + str(val) + ".csv", "tableau_data/")

